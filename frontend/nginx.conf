server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # API reverse proxy al backend (servicio "backend" en docker-compose).
  location /api/ {
    proxy_pass http://backend:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # SPA: todo lo demás cae en index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }

  # index.html NUNCA se cachea: así cada despliegue carga los bundles nuevos
  # (los .js/.css llevan hash y sí se cachean). Evita ver versiones viejas tras deploy.
  location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    expires -1;
  }

  # Cache de assets con hash.
  location ~* \.(?:js|css|woff2?|png|jpg|jpeg|svg|ico)$ {
    expires 30d;
    add_header Cache-Control "public, immutable";
  }
}
