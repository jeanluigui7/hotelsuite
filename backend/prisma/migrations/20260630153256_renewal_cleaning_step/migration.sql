BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Stay] ADD [renewalCleaningStep] INT NOT NULL CONSTRAINT [Stay_renewalCleaningStep_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
