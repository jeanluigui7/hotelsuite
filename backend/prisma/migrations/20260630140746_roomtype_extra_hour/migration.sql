BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[RoomType] ADD [extraHourPrice] DECIMAL(10,2);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
